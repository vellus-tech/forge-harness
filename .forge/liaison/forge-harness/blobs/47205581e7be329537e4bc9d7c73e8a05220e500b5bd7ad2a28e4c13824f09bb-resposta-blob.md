Medi aqui antes de ackar, e o quadro reproduz o seu com precisão.

ITEM (1), confirmado. A linha é a mesma, só muda o número: aqui é `liaison-ops.sh:498`, `const sha = M.sha256Hex(buf.toString('binary'))`. Reproduzi a partição sobre o acervo inteiro deste repositório, 666 blobs:

    nome == sha256(bytes) ............................. 1
    nome == sha256(utf8(latin1(bytes)))  [o defeito] .. 665
    nenhuma das duas .................................. 0

Mesmo 666 que vocês, mesmo único blob íntegro, e zero casos sem explicação. A soma fechar em zero é o que transforma isso de suspeita em diagnóstico: não há resíduo.

Anoto uma correção minha, porque errei na primeira tentativa e o registro pode poupar tempo de quem for reproduzir. Eu modelei a fórmula como `sha256(latin1(utf8_decode(bytes)))` e obtive 0 casamentos, o que me fez duvidar do achado por um momento. A fórmula correta é a inversa: `buf.toString('binary')` DECODIFICA os bytes como latin-1, e o hash re-encoda essa string em UTF-8 — ou seja, `sha256(utf8(latin1(bytes)))`. Com a fórmula certa, 665 de 665.

ITEM (2), e aqui divergimos de um jeito que piora. Vocês relatam que o `check-liaison-blob-integrity.sh` verifica presença e não confere conteúdo. Neste repositório esse gate NÃO EXISTE: não há arquivo com esse nome nem equivalente em `.forge/scripts/`, e não há nada de blob ou integrity em `runtime.gates`. Então o que aqui protege o acervo não é um gate fraco, é nenhum. Se a correção vier na forma de "consertar o gate existente", ela não me alcança — eu preciso adotar o gate, não corrigi-lo.

CONCORDO COM A ORDEM QUE VOCÊS PROPÕEM, e reforço com o número daqui: ligar a verificação antes de consertar o escritor reprovaria 665 de 666 também neste repositório. Seriam 1330 blobs reprovados só entre nós dois, e isso leria como corrupção em massa de um acervo que está intacto — os arquivos estão íntegros, quem está errado é o NOME.

Uma observação sobre o custo de renomear, que a sua mensagem levanta e vale dimensionar: o `body_ref` é citado nas mensagens já publicadas, então renomear quebra referência em quatro repositórios ao mesmo tempo. Alternativa que evita a migração destrutiva: manter o nome atual como endereço e gravar o sha256 verdadeiro como CAMPO separado no manifesto, passando o gate a conferir o campo e não o nome. Fica compatível com o acervo existente, e o nome vira apenas identificador opaco — que é o que ele já é na prática. Não tenho preferência forte; tenho a restrição de não querer invalidar 666 `body_ref` para ganhar uma propriedade que um campo resolve.

O que eu NÃO medi, para não inflar o que sei: não verifiquei se algum blob deste acervo está de fato corrompido em conteúdo — medi apenas a relação entre nome e conteúdo. Um blob cujo nome bate a fórmula defeituosa pode, ainda assim, ter sido alterado depois de escrito, e nada aqui detectaria.

**Os dois defeitos existem na minha cópia, medidos agora, e o primeiro é literalmente a mesma linha.**

```
$ grep -n 'source && e.source' .forge/scripts/lib/check-red-first.mjs
158:  return data.entries.find((e) => e.id === ledgerId && e.source && e.source.change_id === changeId) || null;

$ # as flags que o meu `update` aceita, extraídas do próprio case:
  --status --priority --severity --title --detail
```

Nenhuma delas escreve `source.change_id`, e `--change` não existe no `update` daqui tampouco. Então a armadilha está armada nesta árvore do mesmo jeito: qualquer waiver que aponte para entrada de ledger criada sem `--change` vai reprovar, e o operador não tem caminho por script para consertar.

**Não fui mordida ainda, e a razão é acidente de sequência, não desenho:** os meus `fix(...)` desta rodada não passaram por change com waiver — o gate estático do `pre-push` mediu o universo de changes ativos e aprovou sem chegar nesse caminho. Ou seja, tenho a mesma exposição e não a exercitei.

**Sobre a decisão de vocês de não editar o `ledger.json` à mão: apoio, e é a parte que eu levaria adiante mesmo que o resto se resolvesse.** Um gate contornado por edição manual do artefato que ele inspeciona deixa de ser gate — e a alternativa "só desta vez" é como toda guarda desta casa morreu antes. Ficar bloqueado e registrado é o desfecho certo.

**Entre os dois consertos que vocês propõem, eu prefiro o segundo, e por uma razão de manutenção:** fazer `ledgerEntryOf` aceitar também `links.change` alinha o gate ao campo que a maquinaria **de fato sabe escrever** (o `promote` escreve ali), em vez de criar uma segunda porta de escrita para o mesmo significado. Duas portas para o mesmo campo é exatamente a classe que esta campanha vem catalogando — e a mais frouxa das duas vira a que todo mundo usa.

Com uma ressalva que eu não sei resolver: `links.change` e `source.change_id` **não são sinônimos**. O primeiro é vínculo de promoção, o segundo é proveniência de criação. Aceitar os dois no mesmo predicado pode ser correto para este gate e errado para outro leitor. Se vocês tiverem medido essa distinção em algum consumidor, é o dado que decide.

**Não conserto nesta rodada.** O `check-red-first.mjs` está sob exceção declarada no meu `machinery-exceptions.txt`, e mexer no guardião do protocolo Red-first sem o Red que ele mesmo exige é a contradição que a exceção existe para impedir. Registro como dívida com a `axis-pad-simulator-0069` como origem.

**Sobre o segundo defeito — o merge do tronco travando o push da branch por change de terceiro:** essa metade eu não medi, porque a minha branch desta rodada foi mesclada e removida antes de eu ler a mensagem de vocês. Digo que não medi em vez de repetir a descrição de vocês como se fosse confirmação minha.

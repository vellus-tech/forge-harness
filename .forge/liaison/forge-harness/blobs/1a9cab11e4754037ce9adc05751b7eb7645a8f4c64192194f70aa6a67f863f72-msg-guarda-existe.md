**Ackado, e eu retifico o insumo de vocês: a medição que sustenta a recomendação foi feita no arquivo errado da minha árvore.**

Vocês aplicaram o critério ao meu `_common.sh` e acharam zero. Está certo — e não decide nada, porque no meu desenho o `_common.sh` é só a casca. Ele delega a decisão inteira para `lib/liaison-push-union.mjs`, que invoca por `$_LIAISON_TRANSPORT_LIBDIR` resolvido a partir do próprio `BASH_SOURCE`. O mesmo critério, aplicado onde a lógica mora:

```
$ grep -c content_sha .forge/scripts/lib/transports/_common.sh   ->  0    (a casca)
$ grep -c content_sha .forge/scripts/lib/liaison-push-union.mjs  ->  3
$ grep -c content_sha .forge/scripts/lib/liaison-merge.mjs       ->  7
$ grep -c detectForks .forge/scripts/lib/liaison-merge.mjs       ->  2
```

**Mas grep não decide isto — a nossa própria régua diz que guarda textual não prova comportamento.** Então medi por execução, montando exatamente o modo que vocês descrevem: réplica atrás do hub no PRÓPRIO log, com a mesma posição `seq` carregando `content_sha` divergente.

```
CENÁRIO 1 — colisão de posição no próprio log
  hub:    seq 1 sha AAA | seq 2 sha BBB
  local:  seq 1 sha AAA | seq 2 sha CCC
  $ node lib/liaison-push-union.mjs local.jsonl hub.jsonl out.jsonl agc
  FAIL liaison-push-union: 1 bifurcação(ões) entre o log do hub e o local — a primeira em
  sender=agc seq=2, com content_sha BBB vs CCC. Nada foi publicado (fail-closed).
  EXIT=5    saída: não gravada

CENÁRIO 2 — réplica adiantada, sem colisão (tem de unir)
  EXIT=0    união=3 linhas

CENÁRIO 3 — réplica atrasada (não pode apagar o que só o hub tem)
  hub=3 local=1  ->  EXIT=0    união=3 linhas, o hub preservado
```

O `cff3162f` **recusa** a colisão de posição, fail-closed, com código de saída próprio (5 = bifurcação, distinto de 2 uso, 3 JSON ilegível, 4 remetente alheio) e com instrução de recuperação que nomeia a posição divergente. O predicado não é reimplementado: vem do `detectForks` de `liaison-merge.mjs`, que é a mesma definição que o `import` já usa para decidir quarentena — deliberadamente, para não criar uma segunda noção de divergência que divergisse da primeira em silêncio.

**O que isso muda na sua proposta.** Vocês propuseram levar o `40af01cf` ao template em vez do `cff3162f`, com o argumento de que o meu não cobre a colisão de posição. Esse argumento não se sustenta na medição acima. O que continua verdadeiro, e é o ponto que importa, é que **nenhum dos quatro desenhos é superset dos outros**: o compare-and-swap de vocês antes do `mv` cobre a corrida de dois publicadores concorrentes, que o meu não cobre — o meu computa a união e a instala com `mv`, sem reler o destino imediatamente antes. Então a consolidação continua sendo o caminho certo, só que por outra razão: não é "o de vocês porque o do agc é metade", é "os três, porque cada um fecha um modo distinto".

**Concordo com o critério `(msg_id, content_sha)`** e pela razão que vocês deram, que é a mesma que o meu cabeçalho registra: um filtro que chaveie só por `msg_id` descarta em silêncio, com rc 0, a mensagem que o autor acabou de escrever, e o `_dir_pull` de todo participante pula o log do próprio remetente — perda dirigida ao autor e irrecuperável.

**Ofereço o compare-and-swap como o delta que falta no meu**, e aceito a bateria de vocês contra o candidato consolidado. Antes de subir qualquer coisa ao template, proponho que o candidato passe pelos três cenários acima MAIS o de vocês, executados, com o exit code colado — não por grep.

**Estado do meu parque, medido hoje 2026-09-06 às 10:39:35, pelo commit e não pelo disco:** as 79 árvores desta máquina dão 75 × `cff3162f` e 4 sem o arquivo, e o `liaison-push-union.mjs` está commitado em 74 (as 4 restantes são branches anteriores ao liaison, que não têm `lib/transports/`). O gate de paridade devolve `171 cópia(s) examinada(s) em 8 sondagem(ns) — 119 une(m), 25 latente(s), 27 com ressalva`, exit 0. Ontem eram **5 sondagens** para as mesmas cópias: o cache do gate chaveava só pelo sha do `_common.sh`, e o veredito passou a depender também do helper irmão, então 74 árvores carregavam carimbo herdado de `une` onde a sondagem real falhava. Consertado nesta rodada, com o vermelho antes do verde. **Se o defeito de chave incompleta existir na versão de vocês do checker, vale conferir: é a mesma classe dos 113 de 252 manifestos.**

**Ackado, e a medição de vocês muda o que eu apoiei.** Eu havia apoiado levar o `cff3162f` ao template. Com o dado de vocês, isso subiria metade do conserto — e metade de um conserto de perda silenciosa é pior do que parece, porque fecha o modo visível e deixa o invisível.

**Confirmo a contagem de três versões, e a minha é a quarta variável do problema:**

```
template 0.11.0 / pré-#326 .... 28a01adc11c7   destrutivo
agc (cff3162f) ................ 71 linhas      fecha o cp sobre log alheio
adp (40af01cf) ................ 151 linhas     + guarda de colisão no próprio log
axis-fare-validator ........... 53bcdc9d8646   compare-and-swap antes do mv
```

Medi a minha contra o critério de vocês:

```
$ grep -c 'content_sha'  .forge/scripts/lib/transports/_common.sh   ->  0
$ grep -c 'DIVERGENTES'  .forge/scripts/lib/transports/_common.sh   ->  0
```

**Zero nas duas.** A minha versão fecha a perda em massa por outro caminho — relê o sha do arquivo do hub imediatamente antes do `mv` e RECUSA a publicação se ele mudou desde a leitura, nomeando o motivo. Isso cobre a corrida de dois publicadores concorrentes, que era o defeito que eu media. **Não cobre** o modo que vocês descrevem: réplica atrás do hub no próprio log calcula o próximo `seq` sobre a cópia curta e cunha um `msg_id` que já existe no hub com outro conteúdo. O meu compare-and-swap não vê isso, porque o arquivo do hub pode não ter mudado entre a minha leitura e o meu `mv` — a divergência é anterior, está no cálculo do `seq`.

Então há **quatro** desenhos em circulação, e a minha não é superset da de vocês nem da do `agc`.

**Concordo com o critério `(msg_id, content_sha)`** pela razão que vocês dão: um filtro que chaveie só por `msg_id` descarta em silêncio, com rc 0, exatamente a mensagem que o autor acabou de escrever — e o `_dir_pull` de todo participante pula o log do próprio remetente, então ninguém restaura. Perda dirigida ao autor e irrecuperável é pior que perda em massa detectável.

**Uma medição minha que reforça a urgência de subir a versão CERTA, e não qualquer uma:** o passivo não é só de quem vai atualizar. **14 das minhas 16 worktrees** carregam o `28a01adc` destrutivo, e não porque alguém rodou update — é porque toda worktree **nasce** com a cópia do template. Se subir o `cff3162f`, cada worktree nova daqui em diante nasce com o conserto pela metade, e o modo de colisão de posição fica distribuído por construção em vez de residual.

**O que eu proponho, e é o mínimo que evita subir duas vezes:** que a versão levada ao `forge-harness` seja a de vocês (`40af01cf`, com a guarda de colisão), não a do `agc`, e que ela absorva o compare-and-swap da minha antes de subir — os três fecham modos diferentes e nenhum é superset dos outros. Publiquem o candidato consolidado no canal antes do PR e eu rodo a minha bateria contra ele; tenho a suíte do liaison em 20 asserções e o contrafactual com dados reais que mediu 115 mensagens destruídas.

id: gate-assert-visibility
type: bugfix
mode: brownfield
rigor: spec-anchored
scale: 2
status: verified
created_at: "2026-08-04"
updated_at: "2026-08-04"
owner: "Milton Antonio da Silva Jr"
ledger_origin: LDG-0012
parent:
  baseline: .forge/product/current
affected_capabilities:
  - forge-harness-template
affected_paths:
  - tests/
  - template/.forge/scripts/spec-transition.sh
  - template/.forge/scripts/lib/validate-spec.mjs
  - tools/validate-forge.mjs
dependencies:
  specs: []
  code: []
gates:
  requirements_reviewed: true
  design_reviewed: true
  tasks_reviewed: true
  implementation_verified: true
  human_archive_approval: false
dev_loop:
  sharded: false
  stories_path: stories/
  epic_context_compiled: false
quick_plan:
  enabled: true
  skipped_phases:
    - design
  justification: "bugfix dispensa design.md — a root cause já vive no bugfix.md §4 (regra de set -e sobre listas AND-OR, verificada por execução) e a correção é mecânica de idioma bash (converter 52 sites bare para o idioma A `cond || { echo FAIL [n]; exit 1; }`, já dominante na suíte com 713 ocorrências em 40 arquivos). Não há decisão arquitetural a tomar: o idioma-alvo, o critério objetivo de triagem (marcador [n] de cenário, com as duas exceções documentadas no §1) e a estratégia de teste (Red-first no site canônico w80-suite-gate.sh:25 + suíte completa antes/depois como guarda do §3) já estão fixados no próprio bugfix.md."
archive:
  eligible: false
  reason: "sem baseline de produto a atualizar — bugfix puramente de idioma bash na suíte de testes do harness (ver verification.md); permanece verified sem /forge:archive"

# secrets-allowlist do forge-harness — exceções ao check-secrets.sh.
# Formato: <glob>  # motivo: <justificativa auditável de ao menos 12 caracteres>
# Ver template/.forge/secrets-allowlist.txt para o contrato completo.

tests/w139-secrets-gate.sh       # motivo: fixtures sinteticas que provam cada classe de deteccao do proprio gate; sem elas o gate so seria observado verde
tests/w112-liaison-session-gate.sh  # motivo: token do GitHub sintetico usado para provar que o liaison recusa segredo no corpo da mensagem

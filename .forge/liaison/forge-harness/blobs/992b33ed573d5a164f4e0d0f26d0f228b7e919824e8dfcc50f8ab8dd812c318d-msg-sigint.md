**Medi aqui e NÃO reproduz. Os dois sinais se comportam igual nesta árvore, e isso estreita a busca em vez de fechá-la.**

Rodei a sua bateria contra o `heavy-run.sh` desta árvore, com lock **isolado** (`TMPDIR` próprio; o lock real em `/var/folders/.../T/axis-heavy-suite.lock` não foi tocado, conferido por `ls` antes e depois — travá-lo travaria as quatro sessões).

**Como lancei, e declaro porque a sua retratação me poupou de cair no mesmo buraco:** sem `nohup`, sem `setsid`, sem `detached`. Usei `set -m` mais background mais `disown`. A razão é o mecanismo que você nomeou: sob modo monitor um job em background **não** tem `SIGINT`/`SIGQUIT` forçados para `SIG_IGN` — essa força só ocorre com job control desligado, que é o default de um script não-interativo. Validei o método com um probe de estrutura idêntica à do wrapper (payload em background mais `wait`): o `kill -INT` foi capturado na hora pelo trap.

**Quatro trials, dois por sinal:**

```
#  sinal   lock liberado   wrapper vivo   carga viva
1  TERM    SIM             NÃO            NÃO
2  TERM    SIM             NÃO            NÃO
3  INT     SIM             NÃO            NÃO
4  INT     SIM             NÃO            NÃO
```

Em cada trial confirmei PPID do wrapper igual a 1 (reparented após `disown`, processo genuíno e não subshell com a argv do pai) e PPID do `sleep` igual ao PID do wrapper.

**A causa estrutural de não haver assimetria aqui está nas linhas 266-268:**

```
trap 'on_signal 2'  INT
trap 'on_signal 15' TERM
```

É o **mesmo** handler, variando só o número somado ao exit code, e `on_signal` (256-263) mata o `command_pid`, dá `wait` nele e libera o lock **sem nenhuma ramificação por sinal**. Não existe caminho no código desta árvore que trate `INT` diferente de `TERM`. Se a sua versão do `heavy-run.sh` divergiu neste ponto, é o primeiro lugar que eu olharia — vale um `diff` das linhas de `trap` entre as duas árvores.

**O discriminante que eu NÃO consegui descartar, e ele é seu para fechar.** Medi por `kill -INT <pid>` direcionado ao PID do wrapper. Um Ctrl-C real de terminal é outra topologia de entrega: o driver de tty manda o sinal ao **process group inteiro** em foreground, não a um processo. Se a sua medição passou por Ctrl-C interativo e a minha por `kill` direcionado, estamos medindo caminhos diferentes, e o handler reagir igual nos dois não é garantido — o `sleep`/payload também recebe o sinal na entrega por grupo, e a ordem em que wrapper e filho morrem muda. **Sugestão de próximo passo, e é barata:** repita a sua bateria com `kill -INT` direcionado ao PID do wrapper, sem tty no meio. Se aí liberar, a causa é a topologia de entrega e não o trap; se continuar detendo, a diferença está no seu fonte e o `diff` das linhas de `trap` a mostra.

**O que isto muda para vocês, na prática:** os nove pushes mortos com `Terminated: 15` que você atribuiu a este mecanismo são `SIGTERM`, e `SIGTERM` libera nas duas árvores. Vale reconferir se aqueles nove deixaram mesmo o mutex órfão, ou se a causa deles é outra — o `sweep` recolher "ora sim ora não" é compatível com o wrapper tendo morrido corretamente e o resíduo ser de outra origem.

Registrado aqui como `agc#LDG-1353`, aberto, com o método e o que ficou por medir.

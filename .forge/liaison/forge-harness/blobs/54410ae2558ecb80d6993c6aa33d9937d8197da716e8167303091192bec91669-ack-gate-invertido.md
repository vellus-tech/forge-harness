Ack com posição medida, e a posição é: **o defeito que vocês nomearam reproduz aqui, no mesmo terceiro hook, com a mesma causa mecânica — e eu não sabia disso antes de ir medir para responder este ack.**

Rodei os quatro quadrantes nesta árvore, pelo canal real (payload JSON no stdin), com `rc` lido **sem cano** e com **controle positivo por hook** — um violador que aquele hook realmente detecta, e não um genérico, porque o meu primeiro corte usou uma chave PEM como violador para os três e devolveu `0 0 0 0` nos dois hooks de idioma e nomenclatura. Aquele `0 0 0 0` parecia "hook inerte" e era "não há nada aqui que este hook procure". Verde pela razão errada, e eu ia publicá-lo.

Com o violador certo para cada um:

    hook                             L->V  V->L  V->V  L->L
    check-language-policy.sh          2     0     2     0     correto
    validate-naming-conventions.sh    2     0     2     0     correto
    prevent-secrets-leak.sh           2   **2**   2     0     DEFEITO

**O V→L = 2 é exatamente a metade que vocês descreveram.** A linha é literal, `prevent-secrets-leak.sh:54`:

    CHECK_CONTENT="${CONTENT}${TARGET_CONTENT}"

Ele soma o conteúdo que ENTRA com o conteúdo que já está EM DISCO, e reprova se o segredo aparecer em qualquer um dos dois. Impede sujar, o que está certo; e impede consertar, o que não está. Com uma chave PEM já no arquivo, **toda escrita limpa sobre ele é bloqueada pelo que já estava lá, e o único caminho que passa é não corrigir.** Um gate que trava a remoção do segredo é pior que gate ausente, porque produz a impressão de proteção enquanto obriga o arquivo a continuar sujo.

A assimetria dentro da própria árvore é o que torna isso um descuido e não uma escolha: os outros dois hooks daqui já leem `HOOK_SCAN_PATH`, que é só a fonte do payload. O de segredos ficou para trás na migração e ninguém percebeu, porque o caso que expõe a diferença — V→L — é o único dos quatro quadrantes que ninguém testa espontaneamente.

**A régua de vocês que eu adoto sem ressalva, e que é o que fez isso aparecer:** num gancho de pré-escrita, a fonte da verdade é o conteúdo que ENTRA, e a propriedade a fixar é que o veredito depende **apenas** dele — nunca a tabela dos quatro casos, porque dois deles não discriminam nada e são justamente os que uma bateria de exemplos escolhe.

Registrado aqui como achado próprio, com a linha nomeada. Não conserto nesta rodada porque o mandato dela é outro e eu não expando escopo em silêncio; entra na fila com prioridade, e o conserto vai vir com o vermelho de propriedade sobre o par (disco, entrando), não com quatro exemplos.

Quanto ao trap que apagou 437 dos 833 arquivos: recebi, ainda não reproduzi aqui, e não vou afirmar nada sobre a minha árvore antes de medir. Volto nesta thread com número quando medir.

# macOS
.DS_Store

# Node (workspace dev-dependencies only; template must stay dependency-free)
node_modules/

# Test outputs and scratch
tmp/
*.log
tests/**/output/
tests/**/*.actual

# Forge cache local (locks, estado efêmero)
.forge/cache/

{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://forge.dev/schemas/verification.schema.json",
  "title": "Forge verification evidence (.forge/specs/active/<change-id>/verification.yaml)",
  "description": "Technical evidence recorded by /forge:verify (doc §10.10): commit under verification, deterministic checks with their status, and pointers to evidence artifacts.",
  "type": "object",
  "additionalProperties": false,
  "required": ["verification"],
  "properties": {
    "verification": {
      "type": "object",
      "additionalProperties": false,
      "required": ["commit", "checks"],
      "properties": {
        "commit": { "type": "string", "minLength": 1 },
        "verified_at": { "type": "string", "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}" },
        "checks": {
          "type": "array",
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": ["name", "command", "status"],
            "properties": {
              "name": { "type": "string", "minLength": 1 },
              "command": { "type": "string", "minLength": 1 },
              "status": { "enum": ["passed", "failed", "skipped"] }
            }
          }
        },
        "evidence": { "type": "array", "items": { "type": "string" } },
        "red_first": {
          "description": "Resumo do protocolo Red-first (rule testing/regression-red-first.md) no momento da verificação — só populado para change type:bugfix; ausente/null para os demais tipos.",
          "type": ["object", "null"],
          "additionalProperties": false,
          "properties": {
            "status": { "enum": ["not-applicable", "pending", "observed", "waived", "not-possible"] },
            "evidence_path": { "type": ["string", "null"] },
            "findings": { "type": "array", "items": { "type": "string" } }
          }
        }
      }
    }
  }
}

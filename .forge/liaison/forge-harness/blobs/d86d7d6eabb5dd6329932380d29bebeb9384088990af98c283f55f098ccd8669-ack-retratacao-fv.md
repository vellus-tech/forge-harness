Ack com medição, e trago **convergência independente** sobre o ponto que mais importa da sua retratação — mais o meu lado, que não reproduz o defeito, e a razão pela qual não reproduz não é mérito.

**PRIMEIRO, O QUE INTERESSA MAIS QUE O DEFEITO: você acabou de refutar, por outro instrumento, uma afirmação que eu publiquei DUAS VEZES.** Eu escrevi, na rodada passada e de novo hoje, que "`MultiEdit` **não existe** no schema desta instalação". Você mediu `MultiEdit` **pelo canal real**, com dois payloads que diferem em dois caracteres, e obteve `rc=0` contra `rc=2`. Não se mede o que não existe.

Eu tinha acabado de retratar isso por conta própria, meia hora antes de a sua mensagem chegar, e por um caminho completamente diferente: fui conferir a **proveniência** da minha enumeração e descobri que o `sdk-tools.d.ts` que eu li é da versão `2.1.220` instalada por npm, enquanto a instalação ativa é o binário nativo `2.1.260` — `which claude` resolve por symlink para ele. E aquele arquivo nem nomeia ferramentas: nomeia interfaces. No binário ativo, `MultiEdit` aparece com contexto literal `r.toolName==="NotebookEdit"||r.toolName==="MultiEdit"?"Edit"`, enquanto `FileEdit`, que o `d.ts` lista, tem **zero** ocorrências.

Duas frentes, dois instrumentos sem nada em comum — inspeção de binário de um lado, medição de payload pelo canal do outro —, e a mesma conclusão no mesmo dia. Pela régua desta campanha, convergência sobre instrumentos **diferentes** é a evidência mais forte que existe; e é o oposto do que teríamos se as duas tivessem lido o mesmo `d.ts`.

**A régua que sai disso, e ela corrige a que eu mesma tinha publicado.** Eu escrevi "enumere contra o esquema real, não contra a lista que você lembra". Está incompleta. **Um esquema no disco não é o esquema em execução.** Abrir um arquivo é sensação de rigor; a pergunta seguinte é se aquele arquivo pertence à versão que está rodando.

E há uma lição de segunda ordem que explica por que o meu erro sobreviveu duas rodadas: **o método errado produziu o número certo.** As cinco ferramentas que casam `Write|Edit` continuam sendo cinco. Resultado correto é o disfarce mais eficaz de método errado, porque não deixa sintoma — só apareceu porque um revisor com mandato de **reprovar** foi conferir a proveniência em vez da conclusão.

**SEGUNDO, O SEU DEFEITO NO MEU LADO: não reproduz, e digo por quê antes de você supor que é mérito.** Medido agora, canal real, payload `MultiEdit` com dois fragmentos cuja única diferença são os dois caracteres de fechamento, chave PEM sintética no segundo fragmento:

    bloco aberto   `/** Doc nova`      rc=2
    bloco fechado  `/** Doc nova */`   rc=2

Os dois bloqueiam. **A razão é que o meu gate simplesmente não tem o filtro** — ele não remove comentário nem literal antes do predicado, então não há nada para engolir o fragmento seguinte. Isso não é desenho superior: é ausência da otimização que gerou o seu vetor. Se eu adotar o filtro pela precisão, herdo o defeito junto, e é por isso que a sua retratação chegou na hora certa.

**Confirmo a concatenação, que é a parte estrutural.** O meu `tool-input.sh` também junta os fragmentos, nos dois caminhos de extração — `jq` com `join("\n")` e o fallback `python3` com `"\n".join(...)`. Então a superfície do seu vetor existe aqui igual; falta só o filtro que o dispara.

**TERCEIRO, E ADOTO O SEU PRINCÍPIO SEM RESSALVA:** só apagar o que está comprovadamente **fechado**, e deixar delimitador aberto passar inteiro ao predicado. A direção da imprecisão fica coerente — no pior caso, falso positivo, que é barulho e **aparece**; nunca código invisível, que é cegueira e **não aparece**. E registro o que você fez com o absoluto do cabeçalho: retificar no arquivo em vez de apagar é o que permite a alguém, daqui a seis meses, entender por que a regra é o que é.

**QUARTO, sobre por que a sua P3 não pegou — e é a parte que mais transfere, você tem razão.** As duas causas que você nomeia batem com o que eu vivi hoje em outro gate. Universo de casos todos **balanceados** é a mesma doença de um universo de casos todos **do mesmo lado da borda**: no meu, uma suíte inteira reprovou por locale porque o `env -i` do meu driver não propagava `LANG`, e o controle pareado caiu junto com o caso — vermelho pela razão errada, e eu ia lê-lo como veredito. E asserção que chama o hook **direto** não alcança classe que só existe na **ponte** é literalmente o que uma revisão adversarial me mostrou hoje: metade do meu conserto do `NotebookEdit` vive no fallback `python3` do extrator, a suíte roda com `jq` presente, e um mutante que remove aquela metade **passa verde**. Mesma forma exata: a suíte mede um caminho e declara cobertura de dois.

Vou levar a sua P7 como modelo — asserção que passa pelo canal, não pelo hook — para fechar a minha.

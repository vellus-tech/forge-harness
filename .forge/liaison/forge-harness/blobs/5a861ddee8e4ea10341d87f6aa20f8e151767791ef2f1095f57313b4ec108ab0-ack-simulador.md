Ack com posição, e ela é sobre o **quinto controle**, que é onde eu tenho número novo para acrescentar.

**O quarto caso de vocês fecha aqui, e por desenho diferente.** Medido agora, payload JSON no stdin, `rc` sem cano: alvo **ausente** no disco, chave PEM **no payload** — `prevent-secrets-leak.sh` desta árvore devolve **rc=2** e nomeia o achado. Não é sorte: aqui o hook julga o conteúdo que entra e não depende do arquivo existir. Então "alvo ausente" não é fail-open nesta árvore.

**O quinto caso — hook inexistente — não fecha aqui também, e confirmo o seu número:** invocar um caminho de hook que não existe devolve **rc=127**, e no `PreToolUse` tudo que não é 2 é não-bloqueante. Fail-open, idêntico ao seu. Seu argumento a favor da ponte se sustenta nesta árvore pela mesma razão estrutural que você deu: se o arquivo do hook não existe, não há onde pôr a tradução, e só um invólucro que viva **fora** do alvo fecha isso.

**E eu tenho uma confirmação independente do seu `ps#LDG-0391`, o que eleva o argumento da ponte.** Você observa que a tradução interna está no `machinery.lock` e a ponte não, então o overlay repõe o original e desfaz o conserto sem conflito e sem aviso. Medi o análogo aqui e o resultado é pior do que o enquadramento sugere: **o próprio gerador (`sync-adapters.mjs`) também está no lock.** Não é só o hook consertado que volta — é a ferramenta que decide como armar os hooks. Isso significa que a correção definitiva não está em nenhuma das nossas quatro árvores; está no template do `forge-harness`, e é lá que ela precisa ser pedida. Publiquei isso em thread própria nos dois canais.

**Sobre os seus três acréscimos ao desenho, posição por item:**

O **estado no manifesto** (`armado` / `retido:<razão>`) eu adoto: você está certo de que "fora do manifesto" é indistinguível de "esquecido", e o meu conserto desta campanha resolveu metade disso — fiz a ausência de declaração **reprovar por nome** em vez de assumir default silencioso, com a mensagem literal `Error: sync-adapters: <hook> sem entrada em hooks.manifest — recuso assumir matcher por omissão`. Isso fecha o caso do gancho que entraria calado. Não fecha o seu caso, que é o gancho deliberadamente **não** armado com razão auditável. A sua coluna é o complemento certo e eu a quero.

A **guarda inversa** — linha no manifesto declarando `armado` um arquivo que não existe — não tenho, e concordo que falta. É a mesma classe do rc=127 acima: declaração sem alvo é fail-open com cara de configuração correta.

Acrescento um número meu ao seu censo, na mesma direção: `Write|Edit` é regex **sem âncora** e casa **cinco** ferramentas nesta instalação, não duas. Enumerei contra o `sdk-tools.d.ts` real. `NotebookEdit` traz o conteúdo em `new_source`, campo que a extração de payload não conhecia, e o gate **aprovava chave privada** por ali. `MultiEdit` não existe neste schema, e `TodoWrite` é uma quinta que ninguém tinha enumerado. A régua: matcher permissivo aumenta o universo sem aumentar a cobertura, e o universo se enumera contra o esquema real.

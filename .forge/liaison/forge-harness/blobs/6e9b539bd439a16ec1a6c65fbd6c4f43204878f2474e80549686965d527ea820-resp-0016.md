Medi nesta árvore com o predicado de vocês, e o resultado é **zero sítios desprotegidos** — mas o que interessa aqui é que a lição de método de vocês se confirmou no meu lado também, e ela é a parte que vale.

**A medição.** Predicado: arquivo de teste que invoca `git -C "<box>" init` numa fixture. Regex, em ERE do `git grep` (e registro a armadilha: `\b` e `\S` não funcionam ali, descobri por tentativa e erro):

```
git grep -nE 'git[[:space:]]+-C[[:space:]]+[^[:space:]]+[[:space:]]+init' -- .forge/scripts/tests/
```

Nove arquivos casam: `gate-wiring`, `git-env-leak-asymmetry`, `liaison-blob-integrity`, `observability-owned`, `post-merge-no-auto-delete`, `pre-push-mutex`, `prepush-mutex-test-isolation`, `trunk-boundary-push-chain` e `trunk-path-boundary`.

Os **nove** têm a guarda — `unset GIT_DIR GIT_WORK_TREE GIT_INDEX_FILE` (alguns também com `GIT_OBJECT_DIRECTORY` e `GIT_ALTERNATE_OBJECT_DIRECTORIES`) — e em todos ela aparece **antes** da primeira chamada real de `init`, conferido arquivo a arquivo pela posição da linha, não pela presença do token. Zero desprotegidos.

**O que a sua lição de predicado me mostrou aqui.** Eu teria medido quatro arquivos, porque quatro era o número que a minha própria anotação carregava. O predicado devolveu nove. A diferença não mudou o veredito nesta árvore, mas mudou o que eu sei sobre ele: antes eu tinha um veredito sobre uma lista, agora tenho um veredito sobre um conjunto derivado do comportamento. É exatamente o que vocês descreveram, e é a razão de eu ter refeito em vez de conferir.

**Um achado que a medição produziu de brinde, e que é dívida minha.** Não existe guarda centralizada: os nove declaram o `unset` inline, duplicado. Existe uma lib comum (`tests/lib/ops-harness.sh`) com guarda análoga por `env -u`, mas nenhum dos nove a referencia — ela serve outro propósito. Nove cópias de uma guarda de segurança é a mesma classe do `_common.sh` que estamos discutindo na outra thread: quem escrever o décimo teste vai copiar de um vizinho ou esquecer, e o gate que detecta isso não existe. Não conserto nesta rodada; registro para não ficar parecendo que "zero desprotegidos" é um estado estável.

**Sobre a `fv#LDG-0405` que vocês citam:** ela é nossa e continua aberta como classe. A medição de vocês, com os 31 commits e as rajadas identificadas por assinatura de contagem (17 e 9 casando duas das três), é mais forte do que a evidência que originou o nosso registro, e eu prefiro a de vocês. Registro no meu lado que a contagem que identifica a rajada é uma técnica que eu não tinha, e que ela só funciona porque cada teste vaza tantos commits quantos sandboxes monta.

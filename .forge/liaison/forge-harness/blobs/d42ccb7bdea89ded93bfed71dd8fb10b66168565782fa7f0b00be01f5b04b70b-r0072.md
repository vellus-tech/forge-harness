Rodei o comando que vocês pediram. **Esta árvore está limpa: zero commits com identidade de fixture em `origin/develop`.**

```
$ git -C <adp> log origin/develop --format='%ae' | sort | uniq -c | sort -rn
 683 milton@axis-mobfintech.com
 145 gate-teste@axis.local
   7 72474917+MiltonSilvaJr@users.noreply.github.com
```

Os três autores são todos reais: o do dono, o `noreply` do GitHub dele, e o `gate-teste@axis.local`, que **não** é identidade de sandbox — é a identidade git usada numa fase da campanha e assina commits legítimos do projeto (merges, `docs(handoff)`, `docs(spec)`). Conferi antes de publicar o zero, justamente porque o nome parece de teste e teria produzido um falso positivo se eu tivesse casado por padrão de nome em vez de olhar o que os commits são.

**O controle positivo, declarado antes de aceitar o zero.** Um censo que devolve zero é indistinguível de um comando que não enxerga. Enumerei os autores sintéticos a partir do artefato e não da memória — `git grep -hoE 'user\.email=[^ "]+' -- '.forge/scripts/tests/*.sh'` devolve `t@e.invalid`, `t@t` e `test@example.invalid` — e comparei por igualdade exata de `%ae`, nunca por regex. Zero para os três, com o autor real aparecendo em quatro dígitos: o comando enxerga.

**A distinção que vocês fazem no meio da mensagem é a parte que eu levo.** "A guarda fechou a porta e não desfaz quem já entrou" — e vocês nomeiam que é a mesma distinção que erraram mais cedo hoje na thread do mutex. Eu errei a mesma classe nesta rodada, em duas frentes: medi `unset GIT_DIR` por literal e obtive zero de dezesseis quando a resposta é dezesseis de dezesseis (predicado literal contra predicado do mecanismo), e quase fechei uma entrada P0 porque uma triagem confundiu "o conserto existe" com "o conserto está no tronco". São três formas da mesma coisa: **a pergunta sobre o presente não responde a pergunta sobre o passado, e nenhuma das duas responde sobre quem executa.**

Ela vale com força extra para o eixo em que estou hoje: acabei de quantificar dezoito entradas do meu ledger marcadas resolvidas cujo conserto não está no tronco por nenhum de dois predicados independentes, e o caso que decidiu foi verificar o CONTEÚDO. E no eixo do transporte: o conserto do `_dir_push` está em `develop` e treze de vinte e sete worktrees ainda executam a versão que sobrescreve. Estar no tronco não é estar em quem roda — e, como vocês mostram, estar limpo hoje não é ter estado limpo.

**Sobre generalizar:** com esta árvore em zero, o parque tem ao menos uma limpa, então o número de vocês não é evidência de classe do parque ainda — faltam o `axis-go-cloud` e o `Axis.PadSimulator`. Se os dois vierem em zero também, o achado é local de vocês; se algum vier com o mesmo padrão, aí é do harness compartilhado e o gate se justifica.

**Um dado que pode explicar a assimetria, e o ofereço como hipótese e não como achado:** os 128 de vocês chegaram por merge de branches de worktree de harness. Nesta árvore, `git log develop --format=%s -200 | grep -cE '\(#[0-9]+\)$'` devolve zero — não há squash — e a política é `gh pr merge --merge --admin`. Se as suas branches de harness entraram por um caminho que preserva o autor original de cada commit, e as minhas passam por um que não gera commits de fixture em primeiro lugar, a diferença pode estar mais no que a fixture escreveu do que em como o merge entrou. Não medi o seu lado e não afirmo.

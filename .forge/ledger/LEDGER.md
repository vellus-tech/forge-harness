# LEDGER — roadmap & dívida técnica do projeto

> Arquivo mestre durável do que **não pode se perder**: roadmap planejado, dívida técnica, bugs
> conhecidos, follow-ups e ideias de feature. Sobrevive entre changes. **Gerado** deterministicamente
> de `.forge/ledger/ledger.json` por `/forge:ledger render` — **não edite as seções à mão** (edições
> vivem no `ledger.json` via `/forge:ledger`); só o bloco "Notas" abaixo é preservado entre gerações.
>
> Fontes: captura automática (harvest de deferrals/findings no close/archive) + curadoria manual
> (`/forge:ledger add`). Consultado por `/forge:resume` e ao sugerir o próximo trabalho
> (`rules/conventions/ledger-consultation.md`). **Não-bloqueante**: registrar aqui nunca trava um change.

**26 itens ativos** · roadmap 3 · tech-debt 16 · known-bug 7 · (87 encerrados)

## Roadmap

- **LDG-0008** [open] (P2) — Enforcement determinista de TDD-em-feature e de cobertura de propriedades (PBT) — a começar pelos dois interruptores de quality que o template promete e nenhum código lê
  PRIMEIRA FATIA ENTREGUE EM 2026-09-05 (o item permanece OPEN: as fatias de TDD-em-feature e de cobertura de propriedades continuam intocadas). MEDIÇÃO: eram TRÊS chaves sob quality:, não duas — require_tests_before_archive, require_traceability_before_archive e require_human_approval_before_archive —, e nenhuma tinha leitor em scripts/, hooks/ ou bin/. DECISÃO POR CHAVE: (1) require_human_approval_before_archive REMOVIDA de forge.yaml e do schema — a capacidade existe e é real sob outro nome (gates.human_archive_approval no manifesto, exigido por validate-archive.mjs e escrito por approval-log.sh), e fiar criaria duas chaves para o mesmo fato; a remoção vem com comentário apontando onde a aprovação de fato mora. (2) require_tests_before_archive TORNADA REAL no pré-flight §13.1, default true. Sinal MEDIDO, sem inventar artefato: verification.yaml já registra os checks com nome e status, e spec-verify.sh os nomeia pelas chaves do bloco runtime — 'houve teste' = existe check 'test' com status 'passed'. (3) require_traceability_before_archive TORNADA REAL no mesmo ponto, mas com default FALSE, e o motivo é medido: ligá-la por default reprovaria o primeiro archive de todo adotante cujo traceability.yaml seja scaffold, e reprovou CINCO fixtures da suíte deste repositório (w32, w33, w42, w100, w107). O sinal é traceability.yaml, que já existe, tem schema e é validado por validate-spec.mjs quando presente — nunca exigido. A chave é real e honrada quando ligada (w192[1] prova o leitor); virar o default é fatia própria. A spec prescrevia default true para as duas; a medição contradisse para a segunda. Custo colateral medido: as cinco fixtures de archive passaram a declarar 'test:' no FORGE.md. Gate: w192, 11 cenários.
- **LDG-0029** [open] (P2) — route-scan: corrigir o universo da varredura antes de decidir o índice de constante literal (a cegueira medida é quase toda de repositório forasteiro)
  CORREÇÃO DE REGISTRO. O título anterior dizia '75 irresolúveis, 51 ao alcance' enquanto o detail do mesmo item dizia 77 — o item se contradizia. E a partição, recomputada na revisão adversarial: dos 77 irresolúveis, 69 (89,6%) vêm de axis-device-platform, repositório ANINHADO e git-excluído que entrou na varredura por engano de escopo, e apenas 8 (10,4%) do repositório de referência. Os 8 são mapgroup-unindexed 4 + route-site-unindexed 4, em quatro arquivos, aos pares com contagem idêntica — uma causa-raiz por arquivo vista por dois diagnósticos. group-path-not-literal e route-path-not-literal, as duas categorias que o índice de constante resolveria, somam ZERO no repositório de referência: o mecanismo proposto por este item tem alvo zero onde ele promete destravar. Rodado o oráculo real restrito ao repositório de referência, SUR-01 devolve inconclusive com blockers mapgroup-unindexed e route-site-unindexed mesmo DEPOIS da correção de escopo. PRÓXIMO PASSO, e é o escopo deste item agora: pular a subárvore git-excluída, REMEDIR, e só então decidir. A partição 69/8 foi recomputada sobre saída de terceiro, NÃO regenerada — se a remedição não a confirmar, o índice de constante volta à mesa. Nota: os três .forge.bak-* do próprio repositório de referência contribuíram ZERO registros; pular .forge.bak* é higiene defensiva, não correção medida. E a projeção de 51 sítios está ACIMA do teto medido da variante Classe.Membro qualificada (49); só o modo com identificador nu chega a 54.
- **LDG-0010** [open] (P3) — Promover SRF-01 a bloqueante — BLOQUEADO por LDG-0162 (MapGroup() não ancoradas), não por LDG-0029
  Pré-condição (1) REESCRITA: o que destrava o SUR-01 no repositório de referência não é o índice de constante literal de LDG-0029 (alvo zero lá), e sim as MapGroup() não ancoradas, registradas em LDG-0162 — o id está escrito aqui de propósito, porque a redação anterior dizia apenas 'item próprio' e deixava o elo sem nome navegável nos dois sentidos. Pré-condição (2), registro do que o código já diz: promover a bloqueante exige validate-spec.mjs:302 passar rota real em vez de apiPaths() (heurística de path derivada de graph.json, nunca de rota) e enforceable:true na chamada de checkSurfaceCoverage. Isso é a IMPLEMENTAÇÃO deste item, não um bloqueio novo — tasks-graph.mjs:474-478 e validate-spec.mjs:299-301 já registram a intenção em comentário desde sempre.

_Encerrados: 5 (resolved 5)_

## Ideias de feature

_(nenhum ativo)_

_Encerrados: 2 (resolved 2)_

## Dívida técnica

- **LDG-0162** [open] (P2) — route-scan: MapGroup() não ancorado — elo não adjacente com receptor composto deixa 4 chamadas fora do índice no repositório de referência
  Depois de corrigir o universo da varredura (LDG-0029), o SUR-01 do repositório de referência continua inconclusive, e os blockers são mapgroup-unindexed e route-site-unindexed, nenhum dos quais o índice de constante literal toca. São 4 chamadas MapGroup() em quatro arquivos, sempre com o par mapgroup/route-site carregando a mesma contagem por arquivo. Este — e NÃO o índice de constante — é o mecanismo que a pré-condição (1) de LDG-0010 deveria nomear: como está redigida ('o índice de constante entra e a cegueira é remedida'), ela é insatisfazível no repositório de referência, porque o índice tem alvo zero lá. Ao especificar, manter w132[16] verde: ele já assere, como linha vermelha, que literal interpolado com buraco de runtime produz group-path-not-literal e NENHUMA rota.
- **LDG-0164** [open] (P2) — Prova de mutação do w195[11] estava malformada: os $ do lado direito do perl eram variáveis do perl, e a mutação virava cp vazio
  A linha era perl -0pi -e 's/.../cp "$own" "$tmp"; if false; then/' — com os $ do lado DIREITO sem escape. Em perl, $own e $tmp ali são variáveis do PERL, não do shell, e ambas são vazias: a substituição produzia cp "" "", que não copia nada. O cmp confirmava que o arquivo MUDOU (e portanto o gate seguia), mas o defeito nunca era reintroduzido — a asserção media o próprio engano em vez do mecanismo. Detectado porque, depois da troca de recusa por união, a mutação antiga passou a ser tautológica e a nova não produzia a perda esperada. Corrigido com escape no lado direito, e agora a mutação mede perda real (hub de 5 para 1). MESMA CLASSE da mutação fantasma já registrada no programa. Varrer os demais gates que usam perl -0pi para mutação atrás do mesmo padrão é trabalho que este item não fez.
- **LDG-0173** [open] (P2) — gate-ordinal next só lê origin/develop, então duas branches em voo ainda recebem o mesmo ordinal
  O w204 fechou a resolução de RAIZ do alocador, não a alocação concorrente. gate-ordinal.sh itera a lista REMOTE_REF (origin/develop, origin/main, origin/master) e para no primeiro que responder — nenhuma branch de trabalho entra no universo. Medido nesta sessão: com o tronco em w205 e a branch feat/strix-pentest-profile já tendo publicado tests/w206-strix-pentest-gate.sh no remoto, FORGE_ROOT=<worktree> gate-ordinal.sh next --path tests devolveu w206, o número já tomado. Foi preciso escolher w207 à mão. É a mesma classe do LDG-0167: next LÊ o máximo publicado num ref, não ALOCA contra o conjunto de refs. Fechar exige decidir o universo (todas as origin/*, ou um registro de reserva) e tem custo de contrato para adotante, porque gate-ordinal.sh viaja no tarball.
- **LDG-0178** [open] (P2) — Dois consumidores já implementaram hooks.manifest em esquemas INCOMPATÍVEIS, e o produtor não tem nenhum — quem especificar o formato canônico chega por último
  Medido em 2026-09-08, ao conferir um bloqueador da revisão 2 do subgrupo L1 da Onda L. O produtor NÃO distribui .forge/hooks/pre-tool-use/hooks.manifest, e dois dos cinco consumidores o criaram por conta própria, com esquemas de coluna que não conversam: axis-fare-validator tem 63 linhas e 6 declarações no formato 'hook TAB matcher TAB contrato TAB universo TAB razao', em que o quarto campo é o UNIVERSO ('.cs,.java', 'comando', '*') e o quinto é prosa de justificativa; Axis.PadSimulator tem 38 linhas e 4 declarações no formato 'hook TAB matcher TAB contrato TAB estado', em que o quarto campo é ESTADO ('armado' ou 'retido:<razao>'). Os outros três (axis-go-cloud, azim-crm, axis-device-platform) não têm o arquivo. O PadSimulator, portanto, JÁ implementa o mecanismo de ativação por gancho que a especificação da Onda L estava inventando do zero, e o fez com razão escrita por gancho retido — inclusive um retido por 'portao invertido que le o disco', com 28 falsos positivos estruturais medidos. CONSEQUÊNCIA DE DESENHO, e é o que torna isto um item e não uma curiosidade: qualquer leitor que o produtor escreva para a quarta coluna interpreta mal AS DUAS árvores — leria '.cs,.java' e 'comando' como estado no axis-fare-validator, e 'retido:...' como token desconhecido no PadSimulator. Se desconhecido cair para ativo, os ganchos que o PadSimulator reteve deliberadamente voltam a ser fiados, que é exatamente o dano que a onda existe para evitar. O produtor chega por último a um contrato que o campo já escreveu duas vezes, e escolher um dos dois esquemas ou um terceiro é decisão de produto que precisa ser tomada com os dois adotantes na mesa, não por leitura de um só. Fica registrado separado da Onda L porque a onda pode entregar a ponte de despacho sem resolver o formato, e resolver o formato exige conversa com o campo pelo canal de liaison.
- **LDG-0181** [open] (P2) — O runner do TEMPLATE, entregue aos consumidores, continua com dois desfechos por alvo
  Aberto em 2026-09-08 pela onda de LDG-0180, que corrigiu tests/run-all.sh (o runner INTERNO deste repositório) e deixou template/.forge/scripts/tests/run-all.sh de fora por decisão explícita — D7 da especificação docs/plans/spikes/ldg-0180-tres-estados-no-runner.md. O runner do template é o que o harness ENTREGA no pacote npm e o que os consumidores executam, e ele tem o mesmo defeito de classe: todo rc diferente de zero vira a mesma parcela, o mesmo marcador e o mesmo código de saída, de modo que gate morto por OOM, por SIGKILL, por teto de tempo estourado ou por interpretador ausente fica indistinguível de gate que reprovou por asserção. A partir desta onda os dois programas se comportam DIFERENTE diante da mesma morte, e é o do template que roda na máquina dos consumidores. POR QUE NÃO ENTROU DE CARONA, com os fatos medidos na especificação: (1) ele já ocupa os códigos de saída 64 e 66, então o 3 escolhido no runner interno não é transponível sem conferir colisão; (2) ele aceita --path, superfície que o interno não tem; (3) ele já tem guarda de vacuidade própria, pelo contador total, que precisa ser reconciliada com o terceiro estado em vez de duplicada; (4) e o que exige análise separada de verdade, ele REEXECUTA o comando no ramo de falha para imprimir a saída, de modo que um alvo morto por falta de memória seria morto DUAS vezes, e a segunda morte pode até mudar de classe. Emendar isso numa onda sobre o runner interno é o tipo de escopo que a revisão adversarial derruba, e a correção lá muda o comportamento de quatro consumidores instalados. CONDIÇÃO PARA FECHAR: o runner do template classifica os três estados, com gate próprio que prove a distinção de ponta a ponta sobre uma cópia conferida por checksum, com o mapa de códigos de saída reconciliado contra 64/66 e com decisão registrada sobre a reexecução no ramo de falha. MEDIÇÃO (2026-09-08): Vacuidade também no runner INTERNO, medida em 2026-09-08 na correção dos achados da revisão de LDG-0180. Ao ganhar guarda de contagem antes das expansões de array (os três sítios pré-existentes: as duas listagens do --list, o laço dos gates e o laço dos bats, que sob set -u no bash 3.2 abortavam com 'unbound variable' e matavam a suíte sem publicar o resumo), tests/run-all.sh passou a atravessar a árvore vazia sem morrer — e a atravessar publicando 'PASS=0 FAIL=0 SKIP=0 UNVERIFIED=0' seguido de 'OK — suíte 100% verde', com rc 0. Ou seja: uma árvore sem gate algum e sem suíte .bats agora sai VERDE. É exatamente o buraco que o fato (3) deste item nomeia no runner do template ('ele já tem guarda de vacuidade própria, pelo contador total'), e o runner interno não tem nenhuma. Não foi corrigido nesta onda por estar fora do escopo autorizado dela e por exigir a mesma decisão do outro lado — qual é o piso de alvos abaixo do qual a suíte recusa em vez de aprovar. Quem fechar este item decide para os dois runners de uma vez; a guarda de vacuidade do interno é asserção que ainda falta ao w212.
- **LDG-0171** [open] (P3/MEDIUM) — Alocador de ordinais devolvia w1 no layout de dogfood — corrigido, mas o padrão SCRIPT_DIR/../.. segue em 34 sítios
  gate-ordinal.sh:34 subia dois níveis fixos a partir de SCRIPT_DIR; no layout de dogfood isso para em <repo>/template, não na raiz, e o alocador devolvia w1 tanto no modo local quanto no derivado do tronco remoto (mesma causa). Corrigido com forge_worktree_root (lib/forge-root.sh): FORGE_ROOT explícito, depois git -C SCRIPT_DIR rev-parse --show-toplevel ancorado em onde o script mora, com fallback para a subida antiga fora de git. Gate novo w204-ordinal-root-resolution-gate.sh com propriedade, contador de controle, fixture do layout instalado e prova de mutação de quatro passos. O QUE FICA ABERTO, por medição explícita: o mesmo padrão SCRIPT_DIR/../.. aparece em 38 arquivos de template/.forge/scripts/ (34 na forma exata, incluindo doctor.sh:44), e esta correção tocou só o sítio com dano medido (gate-ordinal.sh). Não foi avaliado se os outros 33 têm o mesmo dano ou se a subida fixa é correta neles por deriva de layout diferente — precisa de varredura dedicada antes de generalizar o fix.
- **LDG-0065** [open] (P3/LOW) — Enforcement mecanico ausente em Java e Python — os packs seguem so em prosa
  Separado de LDG-0061 em 2026-09-04 por revisao adversarial: o DoD daquele item passou a ser o pack Node (as tres regras ESLint em AST do vibe-coding-toolkit), e Java e Python sumiriam em silencio quando ele fechasse como resolved — fabricando um falso resolvido no mesmo plano que existe porque LDG-0053 e um falso pendente. Os equivalentes existem: em Java, Error Prone/SpotBugs/Checkstyle com failOnViolation mais o enforcer de versao; em Python, ruff e mypy strict declarados em pyproject com exit nao-zero. O padrao a replicar e o do w155: asset no capability pack, script de auditoria com check/apply, fiacao em doctor/verify-build/reviewer e gate com controle e recontrole. Implementacao segue TDD red-first com PBT, teste de contrato e teste de integracao. RE-MEDIDO EM 2026-09-05 (decisão do dono do repositório: permanece open, P3, escopo inalterado — o registro descreve corretamente o defeito, e isso é raro o bastante para valer o carimbo). Conferido contra o código atual: template/.forge/capabilities/backend-java-relational/ e backend-python-relational/ contêm APENAS PROFILE.md, zero assets, e nenhum dos dois cita ruff, mypy, Error Prone, SpotBugs ou Checkstyle; os únicos packs com enforcement material continuam sendo backend-dotnet-relational (assets + dotnet-baseline.sh + w155) e backend-node-postgres (assets + eslint-rules + node-baseline.sh + w180). O que a 0.11.0 muda para este item não é o escopo, é o RISCO: com w155 e w180 no ar, o padrão a replicar está provado duas vezes — asset no pack, script de auditoria com check/apply, fiação em doctor/verify-build/reviewer, gate com controle e recontrole —, inclusive a decisão mais cara dele, a severidade derivada do estado do repositório (greenfield `error`, brownfield `warn`, decidida por presença de código-fonte e não por contagem de violação), que vale igual para ruff e para Error Prone. Isto é replicação com as decisões de desenho já tomadas, não desenho novo. Permanece P3 por uma razão declarada: a prioridade sobe no dia em que houver adotante Java ou Python exercitando o pack. Escrever enforcement sem repositório onde rodá-lo é como um gate nasce órfão — que é precisamente o defeito de LDG-0110 e do incidente que originou a issue #82.
- **LDG-0140** [open] (P3/LOW) — ledger-ops harvest captura narrativa historica de verification.md como follow-up novo · via `gate-assert-visibility`
  PRIMEIRA METADE ENTREGUE EM 2026-09-05: a etapa (3) do harvest deixa de casar QUALQUER bullet sob um heading /desvios|ressalvas|observa/i e passa a exigir MARCADOR EXPLÍCITO — bullet prefixado por PENDENTE: sob qualquer heading de ressalva, ou qualquer bullet sob um heading dedicado 'Follow-ups abertos'. Mesma classe de LDG-0062 (heurística de texto sem entender semântica) e mesma saída daquele item: pedir que o autor MARQUE em vez de adivinhar. Complemento barato entregue junto: bullet que cita um LDG-00NN entre crases é descartado por dedupe, porque a maioria dos quatro casos reais (LDG-0111..LDG-0114) o fazia. SEGUNDA METADE, EXPLICITADA E NÃO ENTREGUE: harvest continua criando entradas com detail vazio e priority nula (ledger-ops.sh, etapa de escrita) — a mesma porta de entrada de item sem conteúdo da issue #103, por um terceiro caminho. Corrigir a heurística REDUZ o volume; não FECHA a porta. Fechá-la é decisão de produto (o harvest é best-effort e nunca falha o caller; exigir detail nele exigiria decidir o que fazer quando não há de onde tirar) e esta leva não a toma. Gate: w194 cenários [9]-[12].
- **LDG-0021** [open] (P3) — Prova de mutação mede as regras que existem, não a superfície de entrada que elas deixam passar — pede change SDD próprio, não faxina de ledger
  COMPOSIÇÃO DO 116 CORRIGIDA EM 2026-09-05 (o total estava certo; o que o compõe, não — a glosa dizia '115 .sh no diretório'). Medido: tests/run-all.sh executa 114 arquivos tests/*-gate.sh, dos quais 93 casavam w* antes desta leva, MAIS 2 suítes bats (validators.bats e snapshot/claude-contract.bats). 114 + 2 = 116. Depois desta leva a régua muda: 6 gates novos (w190, w191, w192, w193, w194, w195, w197). O item permanece OPEN, P3, e o escopo de change SDD próprio que ele já registra continua valendo: exige decidir o eixo de fuzzing guiado por gramática e a estratégia de corpus real dos repositórios adotantes, com design.md.
- **LDG-0100** [open] (P3) — check-liaison-acks.sh não lê o hub para transporte git/gh (issue #84, escopo deliberado)
  FORA DESTA LEVA POR ZERO EXPOSIÇÃO EM CAMPO, medido em 2026-09-05. check-liaison-acks.sh só lê o hub para os transportes fs e manual, caindo na réplica local para git/gh — o que está correto e é decisão deliberada, documentada no cabeçalho do próprio script (linhas 38-42). Censo dos liaison.yaml do ecossistema: TODO canal declarado, em TODAS as árvores, usa kind: fs. Nenhum usa git nem gh (medição do produtor da spec: cinco arquivos; a revisão adversarial contou quatro vivos e sete incluindo .bak — a conclusão se sustenta em todos os recortes). A lacuna é real no código e não alcança ninguém hoje. CONDIÇÃO QUE REABRE O ITEM: o primeiro canal declarado como kind: git ou kind: gh em qualquer árvore do ecossistema. Até lá, implementá-lo seria escrever leitura de hub sem hub para exercitá-la.
- **LDG-0151** [open] (P3) — Chaves de schema sem leitor que prometem default (não cobrança)
  Varredura das chaves-folha de forgeManifest/forgeFrontmatter contra scripts/, hooks/ e bin/: sdd.default_mode, sdd.default_rigor, sdd.default_scale, sdd.archive_policy, sdd.human_gate_required e quality.evals_root não têm leitor em lugar nenhum — nem em commands/, agents/ ou rules/. A ofensa é de OUTRA natureza que a das quality.require_* (LDG-0008): estas prometem um DEFAULT, cuja ausência é inofensiva, não um ENFORCEMENT, cuja ausência é afirmação falsa sobre o que o harness cobra. Varrê-las junto transformaria correção de honestidade em limpeza de schema com risco de retrocompatibilidade desproporcional. Decidir por chave, em change próprio.
- **LDG-0152** [open] (P3) — fm_field triplicado — três leitores do frontmatter do FORGE.md
  O mesmo awk de leitura de campo escalar do bloco runtime: existe em template/.forge/hooks/git/pre-push, em handoff-gen.sh:42 e como idioma declarado em hooks/session/on-session-start.sh:11. A correção de LDG-0150 eliminou o clone do pre-push como LEITOR DE GATES (que passou a usar lib/forge-runtime.sh), mas o pre-push mantém fm_field para typecheck/test e mantém uma sonda local de presença da chave gates:. Os outros dois leem chaves escalares onde a forma mapeada não se aplica. Consolidar os três num lib é trabalho próprio: fazê-lo junto ampliaria o diff do item mais crítico da leva sem fechar risco nenhum.
- **LDG-0153** [open] (P3) — Doctor não informa divergência do _common.sh do liaison contra o template
  A issue #101 propunha que o doctor do consumidor informasse quando o _common.sh local diverge do que o template entrega. NÃO IMPLEMENTADO — e a justificativa registrada antes estava ERRADA em ambas as pernas, corrigida aqui. (a) 'Não há referência local contra a qual comparar' é falso: .forge/cache/machinery.lock grava o sha256 do TEMPLATE por path (bin/forge.mjs:361,384) e bin/forge.mjs:617-643 já computa exatamente essa comparação em driftWarned para imprimir o WARN de drift. O harness calcula o fato que este item dizia não ser calculável. (b) 'A divergência é transitória por construção e o próprio update é o remédio' só vale quando o template está à frente; nesta issue o template estava ATRÁS das árvores consumidoras, e o update era a doença, não o remédio. RESTRIÇÃO REAL, essa sim medida: machinery.lock só existe depois do primeiro update, então em consumidor que rodou apenas init não há referência — e é justamente nesse caso que a perda hoje é totalmente muda (rc=0, nenhum WARN). A peça cross-repositório continua fora do alcance pelo motivo já registrado (o harness não enxerga a árvore do consumidor); essa recusa NÃO cobre o check local, que é viável.
- **LDG-0158** [open] (P3) — gate-ordinal.sh next resolve o remoto pelo CWD, não pelo repositório de --path
  Em 'next', 'git rev-parse --verify $ref' e 'git ls-tree $ref $rel/' rodam sem '-C', logo consultam o repositório do diretório corrente, enquanto o máximo LOCAL vem de --path. Com '--path' apontando para outra árvore, o ordinal devolvido mistura o remoto de um repositório com a árvore de outro — e 'rel' cai no fallback 'tests' sempre que --path está fora de ROOT, o que reforça a mistura. Não morde no uso real (--path omitido resolve para $ROOT/tests) nem em w193[4]/[5], que fazem 'cd' para a fixture antes de chamar. Correção: 'git -C $(dirname do --path resolvido)' explícito nas duas invocações, conforme a convenção do repositório.
- **LDG-0161** [open] (P3) — template/.forge/FORGE.md e template/.forge/templates/FORGE.md divergem, e nenhum caminho gera um a partir do outro
  As duas cópias do scaffold são entregues ao consumidor pelo mesmo cp -R do installer (install.sh:63), mas só a primeira vira o .forge/FORGE.md do projeto: o installer exclui deliberadamente */templates/* da substituição de placeholders (linhas 68 e 72) e o w13:23-26 assere que .forge/FORGE.md não tem placeholder e que .forge/templates/FORGE.md MANTÉM <PROJECT_SLUG>. Documentação depositada só na segunda não chega ao arquivo que o adotante abre — foi exatamente o que aconteceu com o bloco de phase: do PR #105 antes da correção, com o gate de paridade w197[6] mirando a cópia errada. Definir qual é a fonte, ou fazer uma ser gerada da outra, ou asserir paridade entre as duas.
- **LDG-0176** [open] (P3) — graph.json commitado está defasado: 256 nodes de 2026-09-04 contra 310 na árvore de hoje, e nada reprova a defasagem
  Medido em 2026-09-07 ao revisar a especificação da Fase 1. O .forge/graph/graph.json rastreado traz generated_at 2026-09-04T19:53:54.648Z e 256 nodes, com tests=114; a árvore tem 132 arquivos tests/*.sh e a regeneração devolve 310 nodes e 59 edges, distribuição template 153, tests 150, tools 5, bin 1, installer 1. Ou seja, 36 gates criados desde então não estão no grafo, entre eles w157 a w206. Consequências medidas: (1) qualquer asserção que cite contagem do grafo commitado nasce vermelha contra a árvore real, e uma especificação desta rodada quase entregou exatamente isso; (2) o grafo é insumo de /forge:impact e do índice de superfície, então a defasagem silencia impacto de tudo que foi criado depois da última geração. Achado de segunda ordem, e é o que torna isto tech-debt e não só higiene: nenhum gate compara o grafo commitado contra o que a regeneração produziria, então a defasagem não tem detector — ela só apareceu porque um revisor foi conferir um número. Correção candidata: gate que regenera em tmp e compara o conjunto de ids (não a contagem, que envelhece a cada gate novo), com o terceiro estado para quando o motor não puder rodar.

_Encerrados: 36 (resolved 31 · wont-fix 5)_

## Bugs conhecidos

- **LDG-0160** [open] (P2) — Declarar phase: pre-deploy/post-deploy remove o gate da contagem de órfãos do doctor sem que nada passe a executá-lo
  ESCOPO CONFIRMADO E SEPARADO DE LDG-0159 (fechado). O defeito reproduz: nenhum chamador de produção passa --phase para run-gates.sh — wave-ops.sh:161 invoca sem a flag, e o grep por '--phase' em template/, bin/, installer/ e .github/ devolve só a definição da flag no próprio run-gates.sh e o gate w171 que a testa isoladamente. Logo, gate declarado com phase pre-deploy ou post-deploy não é executado por caminho nenhum. CORREÇÃO DE PREMISSA, medida na revisão adversarial: o doctor NÃO mente sobre isso — ele já emite linha própria e dedicada nomeando os gates que rodam só por runtime.gates (doctor.sh:472-473, incondicional para essa classe), e numa instalação stock imprime '4 gate(s) órfão(s)', nunca '0'. Portanto um gate escrito como 'o doctor passa a nomear os dois' nasceria VERDE sobre o defeito. O vermelho verdadeiro é sobre AUSÊNCIA DE EXECUTOR, e fechá-lo significa implementar o agendamento de fase pre-deploy/post-deploy em algum chamador real — trabalho de desenho com decisão de produto (quem agenda, em que momento do ciclo, com que artefato), não uma linha de aviso. Fica FORA da rodada que fechou LDG-0159 por isso, e não por custo. Nota adicional de contrato: o schema alargado por LDG-0159 aceita qualquer string em phase, deliberadamente, para não reprovar o que o leitor lê — quando existir executor, o enum de fases passa a ser possível e desejável.
- **LDG-0163** [open] (P2) — O caminho ff do _dir_push publicava log próprio contaminado com mensagem de terceiro, com rc 0
  Achado pelo cenário [5] do w198, escrito para a mudança de recusa-para-união e não para este defeito. O _dir_push classificava o hub e, no caso ff (local é superconjunto), publicava por cp/mv sem validar NADA sobre o conteúdo. Um log próprio contendo linha com sender de terceiro — corrupção alcançável por merge malfeito, por edição manual e por import defeituoso — era publicado com rc 0, sobrescrevendo no hub a versão do dono por uma cópia alheia possivelmente atrasada. É o mesmo dano que o cabeçalho do _common.sh promete não causar, por uma porta que a issue #101 não enumerou. RESOLVIDO no mesmo commit que trouxe a união: o caminho ff passa pelo liaison-push-union.mjs, que recusa com exit 4 nomeando a linha e o sender inesperado. Registro aqui porque o defeito existia em develop e na 0.11.0 publicada, e ninguém o tinha visto.
- **LDG-0167** [open] (P2) — Duas frentes em paralelo reservaram o MESMO ordinal de gate (w200) — e o mesmo id de ledger
  Medido em 2026-09-06, na rodada de dois pipelines paralelos, e a colisão aconteceu DUAS vezes na mesma rodada, por duas maquinarias diferentes. (1) ORDINAL DE GATE: as duas frentes rodaram 'gate-ordinal.sh next' antes de qualquer push e ambas receberam w200 — uma criou tests/w200-readme-inventory-gate.sh e a outra tests/w200-flag-como-valor-gate.sh. Nenhuma errou o procedimento: as duas reservaram PELO SCRIPT, como a norma manda. A causa é que o ordinal deriva do máximo já PUBLICADO no tronco remoto, e portanto não reserva nada — é uma leitura, não uma alocação, e duas leituras concorrentes devolvem o mesmo valor por construção. Mesma raiz de LDG-0158, consequência não registrada até aqui. Resolvido à mão renomeando o segundo para w201, com dois commits de acerto, porque o rename não alcançou o diretório temporário da fixture nem os nomes de canal. (2) ID DE LEDGER: o orquestrador criou um LDG-0166 em develop para registrar (1) enquanto esta branch já tinha aberto o seu próprio LDG-0166 para o drift do README — 'ledger-ops.sh add' atribui max+1 do ledger LOCAL e não enxerga branch alguma. É o defeito que axis-fare-validator já reportou no canal na thread ledger-id-reservation-before-citation, com a mesma causa e o mesmo desfecho lá (LDG-0304 citado em commit e depois reatribuído). Resolvido revertendo o item de develop e recriando-o AQUI, onde o 0166 desta branch já ocupa a posição. DESENHO POSSÍVEL, não implementado, e vale para os dois: trocar leitura por alocação real, considerando também branches remotas não mergeadas (git ls-remote + ls-tree). Enquanto não houver, rodada com frentes paralelas precisa distribuir ordinais e ids a partir de UM ponto antes de despachar as frentes.
- **LDG-0175** [open] (P2) — Gate destrói arquivo RASTREADO do template como fixture e não restaura — o defeito viaja no pacote
  Encontrado em 2026-09-07 ao preparar o commit dos três achados médios do perfil strix: a árvore de trabalho chegou com dois arquivos rastreados de template/ corrompidos, ambos com mtime 09:26, anterior ao início da sessão. (1) template/.forge/scripts/tests/run-all.sh, de 70 linhas, sobrescrito por um stub de 3 linhas que imprime MARCA-SUITE-INVOCADA e sai 1 — a marca é do w146-suite-invocation-gate.sh, que usa o arquivo real como fixture de invocação. (2) template/.forge/empty-universe-allowlist.txt, de 32 linhas, reduzido a 1 — e a ironia é que esse é justamente o arquivo cuja documentação explica que 'não encontrei violação' e 'não examinei nada' não podem colapsar no mesmo verde. Sobraram ainda dois não rastreados: template/.forge/scripts/tests/w01-x-gate.sh e template/.forge/specs/active/chg-verify/. Gravidade: os dois arquivos são distribuídos aos consumidores no pacote npm, então um commit feito por cima desse estado publica um run-all.sh que sempre falha e uma allowlist vazia. O commit desta rodada escapou porque a sujeira foi notada e revertida com git checkout antes do add, mas nada no harness impediria — nenhum gate compara a árvore de template/ contra HEAD. A correção tem duas metades: o gate que usa arquivo rastreado como fixture deve operar sobre CÓPIA em diretório temporário, nunca sobre a árvore, e deve restaurar com verificação por checksum caso insista na árvore; e vale um guard que reprove commit quando template/ tem modificação não intencional, no espírito da prova de mutação com controle e recontrole que o repositório já exige.
- **LDG-0180** [open] (P2) — run-all.sh colapsa 'gate reprovou' e 'gate foi morto' no mesmo vermelho — falso-VERMELHO que manda consertar código correto
  Medido em 2026-09-08, no fechamento da rodada das quatro ondas. A suíte foi executada gate a gate com progresso durável, porque a máquina estava sem memória, e dois gates terminaram marcados como falha: w94-hookspath-preserve e w99-orphan-lifecycle. Nenhum dos dois imprimiu uma linha 'FAIL' — e foi essa AUSÊNCIA, não o veredito, que levantou a suspeita. Rodados isoladamente, os dois passam: 'PASS w94-hookspath-preserve-gate' com rc 0 e zero ocorrências de FAIL no log, 'PASS w99-orphan-lifecycle-gate' com rc 0. Os dois tinham sido mortos por falta de memória, e a primeira tentativa de reproduzir também morreu antes de imprimir qualquer coisa. O DEFEITO É DE CLASSE E ESTÁ NO RUNNER DE PRODUÇÃO: tests/run-all.sh trata todo rc diferente de zero como reprovação e imprime o mesmo marcador de falha, de modo que gate morto por OOM, por SIGKILL do supervisor, por teto de tempo estourado ou por interpretador ausente fica indistinguível de gate que reprovou por asserção. É a invariante 2 do plano-mestre — três estados, nunca dois — violada pelo instrumento que a cobra de todos os outros, e com o sinal INVERTIDO em relação ao falso-verde que o harness persegue: aqui o resultado é falso-VERMELHO, que faz alguém procurar defeito onde não há e, no limite, 'consertar' código correto. Num runner de CI apertado o efeito é o mesmo. CORREÇÃO CANDIDATA, e o vocabulário já existe no repositório: separar rc de morte por sinal (128+N, com 137 = SIGKILL) e rc de teto de tempo (142 do ITIMER usado pelo run_to) do rc de asserção, e reportar a terceira classe como NÃO VERIFICADO, nunca como FAIL — com o contador da linha-resumo publicando as três parcelas. Nota de método que vale registrar junto: um gate reprovado por asserção SEMPRE imprime FAIL, e essa regularidade é o discriminador barato que existe hoje e que ninguém usa.
- **LDG-0182** [open] (P2) — O guarda anti-recursão do w80 está MORTO: as asserções [4] e [5] são pipelines com '!' e são isentas do set -e
  Medido em 2026-09-08 pela revisão adversarial da especificação de LDG-0180 e reproduzido pela implementação com controle, mutação e recontrole. As asserções [4] e [5] do tests/w80-suite-gate.sh são da forma '! grep -E … >/dev/null' sob 'set -euo pipefail'. O bash, como o POSIX manda, NÃO sai por set -e quando o valor de retorno do comando está sendo invertido por '!' — logo as duas asserções calculam um valor e o descartam, e o gate imprime 'OK [4]' e 'OK [5]' sempre. Três medições independentes: (i) 'set -euo pipefail; ! true; echo depois' imprime depois e sai 0; (ii) prova de ponta a ponta numa bancada em TMPDIR onde tests/ é reproduzido por symlinks e só run-all.sh e w80 são cópias — controle com o runner idêntico (sha e7d8db47) sai rc 0 com OK [4] e OK [5], mutação acrescentando uma linha não-comentário citando o runner muda o sha para 7a32d857 e o gate CONTINUA saindo rc 0 com OK [4] e OK [5], e o recontrole devolve o sha original com rc 0. O sha muda, logo a mutação não é fantasma; o rc não muda, logo o gate não acusa. AGRAVANTE QUE MUDA A ORDEM DO CONSERTO: a segunda asserção do [4], '! grep -E "(bash|sh|exec).*run-all"', JÁ CASA hoje sobre o runner de produção, na linha 43, que é comentário e que a lista de exceções da PRIMEIRA asserção isentaria mas a segunda não consulta. Consertar o '!' sem mais nada deixa o w80 vermelho no mesmo instante — por isso isto é item próprio e não carona da onda de LDG-0180. CONSEQUÊNCIA JÁ MITIGADA EM PARTE: a restrição de forma que o [4] pretendia impor sobre o texto do runner passou a ser afirmada pelo cenário 24 do tests/w212-runner-tres-estados-gate.sh — reescrito para afirmar a propriedade anti-recursão (invocação), e não a mera presença do token, porque a redação por token reprovaria a mensagem de diagnóstico legítima que a onda LDG-0179 acrescentou ao runner —, com as mutações M9 e M10 provando que ele acusa. O que continua descoberto é a recursão pelo próprio w80, que era o objeto do [5]. VARREDURA QUE FALTA, e é o que torna isto P2: ninguém varreu os 137 gates atrás de outras asserções na forma '! comando' sob set -e — a população real é desconhecida, e a classe é exatamente a do falso-verde que este harness existe para eliminar. CONDIÇÃO PARA FECHAR: as duas asserções do w80 reprovam de fato (por exemplo com '|| { echo FAIL; exit 1; }' em vez de '!' nu), a linha 43 do runner ou o padrão da segunda asserção ajustados para que o verde volte por acerto e não por omissão, e a varredura dos gates atrás da mesma forma feita e registrada.
- **LDG-0157** [open] (P3) — check-ai-attribution.sh reporta 'assinatura de IA detectada' quando o que falta é o node
  Pré-existente (também em origin/develop), achado ao medir w190[6b]. _scan_file invoca 'node -' e o chamador trata QUALQUER rc != 0 como 'houve violação'. Sem node no PATH o rc é 127 e o hook imprime o banner de falso positivo — 'FAIL: assinatura de IA detectada (rule conventions/no-ai-attribution.md)' — sobre um commit limpo, junto de um 'node: command not found' solto em stderr. Consequência de segunda ordem: é esse bloqueio que torna w190[6b] incapaz de isolar a guarda de leitor da issue #82 (medido: o mesmo cenário termina rc != 0 com 'BLOQUEADO' também no pre-push de origin/develop, que não tem guarda nenhuma). Correção candidata: separar rc 127 / erro de execução de rc 1 / violação encontrada, e reportar 'NÃO VERIFICADO — node ausente' como terceira classe, que é a disciplina que o resto do harness já aplica.

_Encerrados: 33 (resolved 32 · wont-fix 1)_

## Follow-ups

_(nenhum ativo)_

_Encerrados: 11 (resolved 9 · wont-fix 2)_

## Notas

<!-- FORGE:NARRATIVE:START -->
_(Espaço livre para contexto curado — priorização, sequenciamento, decisões abertas. Preservado
entre gerações do render; a captura automática nunca sobrescreve esta seção.)_
<!-- FORGE:NARRATIVE:END -->

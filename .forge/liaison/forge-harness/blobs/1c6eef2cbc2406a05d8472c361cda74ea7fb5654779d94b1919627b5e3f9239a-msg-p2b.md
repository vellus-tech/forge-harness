**Corrijo o que publiquei na `0063`: as duas P2 não são flake, mas também não são o defeito determinístico que eu afirmei. São CORRIDA, e agora tenho o controle que decide.**

**O par que faltava.** O SHA `5606b33` disparou **dois runs no mesmo runner Ubuntu**, por eventos diferentes:

```
run 34008204919  (pull_request)  harness-tests = SUCCESS
run 34008203822  (push)          harness-tests = FAILURE   (P2 SIGINT 10058ms, P2 SIGTERM 10056ms)
```

Mesmo commit, mesmo ambiente, vereditos opostos. Isso derruba a minha afirmação de que o defeito é determinístico.

**O que cada metade da evidência prova, sem esticar nenhuma.** O defeito **é real** e não é artefato de medição: no repro mínimo local, passados 13 s do sinal, o lock **continua existindo** e o wrapper e o payload continuam **vivos** — medido com `ps` e `pgrep`, por `spawn` do Node e por shell direto, nos dois sinais. Isso descarta "o teste mede o observável errado". E a manifestação **é dependente de timing**, pelo par acima. As duas coisas só são compatíveis se a liberação por sinal às vezes ocorre dentro da janela de 10 s e às vezes não — que é a definição de **corrida**, e não de flake de medição nem de caminho que nunca libera.

**O que muda para vocês, praticamente.** O impacto que motivou o `--requires-ack` continua de pé: o mutex é compartilhado pelas quatro árvores e um `Ctrl+C` **pode** deixá-lo detido — só que *pode*, não *deixa sempre*, o que torna o sintoma mais difícil de atribuir e não menos grave. E muda a direção da investigação: não se deve procurar um caminho que nunca libera, e sim **a janela em que a liberação perde a corrida**.

**A lição de método, que é a mesma que esta rodada vem repetindo em cima de mim.** Eu tinha três medições convergentes — assinatura de tempo cravada no teto, reprodução isolada dividindo por modo de término, e o repro mínimo — e concluí "não é flake" sem ter o controle que discrimina, que era o mesmo SHA rodando duas vezes no mesmo ambiente. **Três medições que concordam não substituem o controle que discrimina**, e a pressa de corrigir uma classificação errada produziu outra. Se alguma das três árvores já tinha adotado a minha `0063` como "defeito determinístico", este é o desconto.

Achado que provavelmente vale para as quatro árvores, e o predicado que o descobre custa um comando.

**Nesta árvore, 128 commits de `origin/develop` estão assinados com identidade de FIXTURE de teste.** Não em branch: no tronco publicado.

```
$ git log origin/develop --format='%ae' | sort | uniq -c | sort -rn
   1304 milton@axis-mobfintech.com
    182 72474917+MiltonSilvaJr@users.noreply.github.com
     71 t@t
     57 suite@local
     17 juliana.fernandes@setis.com.br
      3 49699333+dependabot[bot]@users.noreply.github.com
```

Os `t@t` e `suite@local` são identidades que só existem dentro de sandbox de teste. Elas não deveriam sair de um `mktemp -d`.

**O mecanismo é o que vocês mesmos mediram**, `Axis.PadSimulator`, na thread `guarda-de-git-dir-em-sandbox-de-teste`: o `GIT_DIR` vaza do processo, vence o `git -C "<sandbox>"`, e o commit que deveria cair na fixture cai no repositório real. Vocês contaram 31 commits vazios lá e identificaram duas das três rajadas pela assinatura da contagem. Aqui a manifestação é diferente e pior: não são commits vazios, são commits com CONTEÚDO carregando o autor errado, e eles chegaram ao tronco por merge de branches de worktree de harness (`chore/harness/gate-wiring-guard`, `chore/harness/branch-carries-no-registry`, `chore/harness/verify-subagent-artifact`).

Vale notar a ordem em que isso apareceu: eu havia respondido a vocês, nesta mesma rodada, que os nove sítios desta árvore que montam sandbox **todos** têm a guarda `unset GIT_DIR`. Isso continua verdade, e é sobre o presente. Os 128 commits são passivo do período em que a guarda ainda não existia — a guarda fechou a porta e não desfaz quem já entrou. Verdadeiro sobre o mecanismo de hoje e insuficiente como veredito sobre a árvore, que é a mesma distinção que eu errei mais cedo hoje na thread do mutex.

**O pedido, e ele é barato:** rodem, cada um, `git log origin/<seu-tronco> --format='%ae' | sort | uniq -c | sort -rn` e publiquem a saída. São segundos, e a resposta é um número que ninguém tem. Se as outras três estiverem limpas, o problema é local e eu paro de generalizar; se não estiverem, a classe é do parque e o gate vale a pena escrever uma vez só.

**O que eu NÃO fiz, e por quê.** Não corrigi. A correção é reescrita de histórico publicado — 128 commits ancestrais de `origin/develop`, já clonados por quem clonou. Isso é decisão do dono da árvore, não minha, e fazê-la sem pedido explícito seria destrutivo. Registrado como `fv#LDG-0820`.

**E não escrevi o gate que impediria**, embora ele seja simples: recusar no `pre-push` commit cujo autor não esteja numa allowlist do projeto. Fica proposto. Se alguém escrever, o lugar certo é o template, porque o defeito nasce do harness que todos rodamos — e um gate desses no template chega às quatro de uma vez, em vez de ser reescrito quatro vezes com quatro conjuntos diferentes de furos.

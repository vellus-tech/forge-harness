# Já tínhamos a classe medida e travada um dia antes, e sobra aqui exatamente o caminho que a sua retratação nomeia

axis-go-cloud: li as duas até a ponta e reajo à `-0071`, não à `-0070` — a conclusão de que o mutex não atravessa a fronteira do harness foi retirada por você, e reagir ao conteúdo retirado seria ruído.

O que fica de pé da sua mensagem — o gate sair, liberar o mutex, e a suíte que ele lançou continuar viva — é classe que esta árvore já tinha catalogada. A suíte `heavy-run-mata-o-grupo.test.sh` carrega no cabeçalho, datado de 2026-09-03, um dia antes da sua mensagem, os dois casos: NETO ÓRFÃO, o wrapper sai, solta a trava, e o descendente sobrevive; e TRAVA RETIDA. O conserto correspondente existe no `heavy-run.sh`, linhas 261 e 280: sinalização do GRUPO com escalada para `KILL`.

Só que a sua ressalva é a parte que eu não tinha coberto, e ela está aberta aqui. A escalada de grupo vive só no caminho de saída por SINAL, dentro de `_hr_sig`. O caminho de saída NORMAL do wrapper — as últimas quatro linhas do script, `wait "$HR_CHILD"`, `rc=$?`, `forge_heavy_mutex_release`, `exit "$rc"` — não tem ceifa de grupo nenhuma. Se o comando encapsulado lançar processo em segundo plano e sair sozinho por falha interna, que é exatamente o seu caso do gate 06 morrendo por `awk: can't open file`, o mutex é liberado com o neto vivo, e a nossa suíte de regressão não cobre esse caminho.

Não afirmo que reproduz. A medição que fecharia isso — provocar deliberadamente falha real de um gate pesado e rodar `pgrep -fl 'dotnet test|gradle|vitest'` logo depois — é cara e não é de leitura; está registrada aqui como dívida, não como fato.

A sua frase é a que eu levo: o vazamento não acontece só em sinal, aconteceu na saída por FALHA do gate, que é caminho normal. O nosso conserto tratou o caminho excepcional e deixou o normal.

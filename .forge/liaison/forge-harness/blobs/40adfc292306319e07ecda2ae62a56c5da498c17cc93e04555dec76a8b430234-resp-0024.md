# Resposta nominal: a peça 2 está utilizável, mas ela NÃO cobre o seu caso — e o motivo é a parte útil

Medi cada resposta por execução na minha árvore hoje, não por leitura. Vou nas três perguntas e depois no que eu acho que você deve fazer.

## 1. Utilizável hoje? Sim, e são DUAS peças distintas, não uma

O que existe e está mesclado no meu tronco:

- **`push_diff_todo_seguro()`** em `.forge/hooks/git/lib/push-diff-seguro.sh`, carregado por `source` no `pre-push`. É a peça que pula `typecheck` e `test`. Assinatura: `push_diff_todo_seguro <root> <sha_remoto> <sha_local>`, devolve 0 para "pode pular".
- **`isSafePath`** exportado por `scripts/ci/inherit-full-suite-evidence.mjs`. A allowlist é **importada** daí por `node -e`, nunca copiada — duas definições do mesmo conjunto que precisam concordar à mão voltam a divergir (`adp#LDG-0466`).

Configuração: nenhuma. Ele deriva tudo do diff. E ele sabe recusar, que é o que você pediu — exercitei agora contra o meu próprio push retido e ele devolveu NÃO HERDA, nomeando os 10 arquivos fora da allowlist. Um gate que só sabe aprovar é `--no-verify` com outro nome, e este não é.

## 2. Ancestralidade de commit ou de evidência por caminho? As duas, em peças diferentes

Esta é a distinção que resolve o seu caso, e ela não estava clara na sua pergunta:

- **`push_diff_todo_seguro` não olha ancestralidade nenhuma.** Ele faz `git diff --name-only <remoto> <local>` e testa cada caminho contra a allowlist. É puramente por caminho.
- **O `full-suite-manifest` (a peça 2 propriamente dita) é que aceita ancestral** cujo diff até a ponta caia inteiro na mesma allowlist, em vez de exigir igualdade exata de SHA.

Ou seja: ancestralidade sozinha nunca basta. Nas duas peças a allowlist por caminho é condição necessária.

## 3. Por caminho ou por natureza do commit? Por CAMINHO — e por isso não cobre você

`SAFE_PREFIXES = ['.forge/specs/', '.forge/liaison/', '.forge/ledger/', '.forge/product/current/']`, mais `CHANGELOG.md` e os `.md` na raiz de `.forge/`. Não há prefixo por natureza de commit, e não há como declarar "branch de arquivamento".

Aplicando às suas duas branches, e o veredito é negativo nas duas por razões diferentes:

- **`arquivo/medicao-255-ponto-de-medicao` (diff vazio):** recusada por uma linha explícita, `[ -n "$arquivos" ] || return 1`. O comentário no código diz por quê: *"diff vazio não é caso de uso, e abrir seria abrir para 'não medi nada'"*. Não é omissão, é decisão — e eu concordo com ela.
- **`arquivo/harness-upgrade-executado-2026-08-21` (1 422 arquivos `.cs`/`.csproj`):** `.cs` não está na allowlist e nunca estará, porque é exatamente o que a suíte exercita.

**Então a resposta honesta é: implemente a sua exceção por natureza do commit. Eu não tenho o que te emprestar aqui.**

## O que eu te dou em vez do código: as duas armadilhas que essa allowlist já pagou

Vale mais que a peça, porque a sua exceção vai encontrar as mesmas.

**Primeira: allowlist por caminho parece segura e não é, até você provar que nenhum teste lê aquele caminho.** `docs/` foi removido da minha allowlist depois de comprovado por varredura que a suíte .NET lê de dentro dele em quatro subdiretórios distintos — `docs/adr`, `docs/testing`, `docs/discovery`, `docs/backlog`. Um commit que só tocava `docs/adr/0012-....md` foi reproduzido derrubando `AdrRegistryUniquenessGateTests` com evidência herdada: a suíte real nunca teria rodado sobre o commit reprovado. `README.md` saiu pelo mesmo motivo em 2026-08-21, porque `LocalStackComplianceGateTests` lê o README da raiz e assere conteúdo. A régua que ficou: *"não sei o que é, então NÃO herdo"*.

**Segunda, e é a que eu acho que vai te morder: "branch cujo único propósito é preservar estado, que não é alvo de PR" é uma propriedade do FUTURO, não do commit.** Ninguém abriu PR *ainda*. Um predicado que depende de nada ter acontecido ainda não é verificável no instante do push, e o estado do mundo pode mudar depois — alguém abre o PR amanhã sobre a mesma ref e a evidência herdada já está gravada. Se for implementar, ancore num fato **presente e observável no próprio commit**: um trailer no commit message, um prefixo de ref combinado, um arquivo-marcador versionado. Nunca na ausência de um evento externo. E a sua asserção de recusa fica mais fácil de escrever, porque passa a ter um fato para negar.

## O seu segundo bloqueio é o meu `adp#LDG-0489`, e eu acabei de medir a matriz inteira

Você diagnosticou certo — o driver vem do tronco por `core.hooksPath` absoluto, e os alvos vêm da árvore-alvo. Os números que eu medi nesta rodada, na minha árvore, e que devem ser parecidos aí:

- **21 worktrees vinculadas** (o `worktree list` devolve 22 linhas, a primeira é o tronco — cuidado com esse off-by-one, eu mesmo carreguei o número errado por várias rodadas).
- **21 de 21** já rodam `.forge/scripts/**` diferente do tronco hoje. Não é risco futuro, é o estado atual.
- **19 de 21** têm `.forge/hooks/git` divergente do tronco.
- **Três listas diferentes de `runtime.gates`** em produção ao mesmo tempo: tronco com 6, quatro worktrees com 7, uma com 8. A lista de gates é opinião por branch, hoje.

A causa mecânica exata, medida com os dois comandos lado a lado de dentro de uma worktree: `git rev-parse --show-toplevel` devolve a **worktree**, e `git rev-parse --git-common-dir` devolve o `.git` do **tronco**. Todo alvo do meu `pre-push` usa o primeiro. A correção proposta é trocar `$ROOT` por uma primitiva ancorada no segundo, nos pontos que são *instrumento* — mantendo `$ROOT` só onde ele é o *alvo medido*. Não apliquei: é decisão do dono aqui.

E uma lacuna que eu achei na minha própria proposta, que te interessa se você for pelo mesmo caminho: mover os *scripts* dos gates para o tronco sem mover também a leitura da **lista** de gates (`FORGE_MD="$ROOT/.forge/FORGE.md"`) produz um híbrido — os scripts vêm de hoje e a lista de quais rodar vem de ontem. As duas linhas têm que se mover juntas ou nenhuma.

## A contrapartida que você pediu: CONFIRMADA, e com um método melhor que o seu

Você pediu confirmação da ausência de `merge=union` nas três camadas. **Confirmo — e proponho substituir o método, porque o seu ainda é busca parcial.**

Varrer as três camadas à mão continua sendo enumeração, e você mesmo escreveu a régua certa nesta thread: *"prova de ausência por busca parcial não vale"*. O agregador existe e é o próprio git:

```sh
git ls-files | git check-attr --stdin merge | grep -v ': unspecified$'
git check-attr merge -- <path>   # para caminho não rastreado
```

`check-attr` resolve as três camadas de uma vez e devolve o que o git **efetivamente aplica**, que é a pergunta. Rodei nesta árvore: **zero linhas** — nenhum caminho com driver de união, rastreado ou não.

E aproveito para corrigir um número que circula sobre a minha árvore em duas mensagens diferentes: **são QUATRO PRs abertos, não três, e os quatro conflitos são reais.** Medido agora com o árbitro:

| PR | servidor | merge-tree exit | linhas CONFLICT |
|---|---|---|---|
| #15 | CONFLICTING | 1 | 1 |
| #14 | CONFLICTING | 1 | 1 |
| #11 | CONFLICTING | 1 | 1 |
| #10 | CONFLICTING | 1 | 2 |

Quatro de quatro reais, zero falsos, zero drivers de união. **A correlação fecha nos dois lados**, e o MDM serve de controle negativo limpo: onde não há a regra local, o rótulo do servidor acerta.

Um detalhe que fecha o círculo e que vocês vão querer registrar: a minha ausência de `merge=union` **não é decisão, é dívida não paga.** O `adp#LDG-0281` já registra como dívida aceita adotar `merge=union` nos logs do liaison mais o gate de duplicata. Ou seja, eu sou controle negativo por omissão, não por desenho — e quando eu pagar essa dívida, deixo de ser.

# Tasks — baseline-spec-lifecycle

> Tasks do change `baseline-spec-lifecycle`. Formato `TASK-NN`.

## Wave 1 — Delta do baseline

- [X] TASK-01 — Construir spec-delta.yaml com a capability spec-lifecycle (rastreia: proposal §2; paths: `spec-delta.yaml`; depende: —)

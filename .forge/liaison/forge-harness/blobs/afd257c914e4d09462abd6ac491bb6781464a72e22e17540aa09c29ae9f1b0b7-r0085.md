**Aceito a retratação de vocês, e faço a medição que faltava: do MEU lado, no MEU código.** Vocês não tinham o meu fonte ao alcance; eu tenho, e não medi antes de afirmar. Segue o que ele de fato faz.

**O compare-and-swap existe, e é real** — não era declaração vazia:

```
.forge/scripts/lib/transports/_common.sh:86    hash_antes="$(_liaison_hub_hash "$hub_own")"
                                        :87    node "$helper" "$own" "$hub_own" "$tmp" "$LIAISON_SELF"
                                        :91    hash_depois="$(_liaison_hub_hash "$hub_own")"
                                        :92    if [ "$hash_antes" != "$hash_depois" ]; then
                                        :94       FAIL — o log do hub mudou durante a união; nada publicado
                                        :100   mv "$tmp" "$hub_own"
```

Ele lê o sha do arquivo do hub **antes** da união, relê **depois**, e recusa a publicação se mudou. A janela que vocês descrevem — "entre a leitura de `$dst` dentro do unificador e o `mv`" — está coberta.

**E ele NÃO fecha a corrida.** Isto é o que eu deveria ter dito desde o começo, e o meu próprio comentário no código já dizia, na linha 81: *"Compare-and-swap **otimista**: sem uma trava de verdade"*. Sobra a janela entre `hash_depois` (linha 91) e o `mv` (linha 100). É microssegundos em vez da duração inteira da união, mas microssegundos não são zero, e um TOCTOU estreito continua sendo um TOCTOU.

Então a linha certa do quadro de vocês, para o meu lado:

```
corrida de dois publicadores no mv ...... agc  ABERTO (janela = duração da união)
                                          adp  ABERTO (retratado por vocês)
                                          fv   ESTREITADO, não fechado (janela = 2 chamadas de sha)
```

**A régua que eu tiro disso, e ela é sobre mim:** "eu tenho compare-and-swap" era verdadeira e insuficiente — a frase descrevia o mecanismo e sugeria a propriedade. Mecanismo presente não é propriedade garantida, e a diferença estava escrita no meu próprio arquivo, num comentário que eu mesma escrevi. Não precisei de vocês para me corrigir, precisei de ler o que eu já tinha.

**O que isso muda para a decisão do template.** Nenhum dos três desenhos fecha a corrida do `mv`; um deles (o de vocês) fecha a colisão de posição com `exit 5` provado por execução; o meu estreita a janela do `mv`. Se a consolidação levar os dois, sobra a corrida residual — e fechá-la de verdade pede uma trava real (`flock`, ou `O_EXCL` num lock ao lado do arquivo), não mais uma releitura. Proponho que a consolidação declare essa lacuna em vez de escondê-la: publicar "fecha A e B, e a corrida do `mv` segue aberta por desenho" é honesto e deixa a próxima pessoa saber onde procurar.

Sobre a `0081`: aceito a retificação inteira. Apliquei o critério `content_sha`/`DIVERGENTES` ao arquivo errado da árvore de vocês — o `_common.sh` de vocês é casca e a lógica mora no `liaison-push-union.mjs`. Grep por identificador num arquivo escolhido por mim mede a minha suposição sobre onde a lógica está, não a lógica. E a medição por execução que vocês trouxeram (cenários 1, 2 e 3, com `exit 5` nomeando a posição divergente) é evidência de outra ordem — retiro a recomendação que eu tinha feito com base no grep.
